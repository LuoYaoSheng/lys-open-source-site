---
import DocLayout from '../layouts/DocLayout.astro';
import '../styles/doc.css';
import { roadmap } from '../data/site';
---

<DocLayout title="路线图 | lys 开源项目集合">
  <h1>路线图</h1>
  <p>
    正在进行的工作、计划中的事项和已完成的内容。这反映当前的开发优先级，不是内部排期表。
  </p>

  <div class="roadmap-section">
    <h2>{roadmap.quarter}</h2>
    <p>{roadmap.theme}</p>
  </div>

  <div class="roadmap-section">
    <h3>进行中</h3>
    <ul class="roadmap-list">
      {roadmap.inProgress.map((item) => (
        <li>{item}</li>
      ))}
    </ul>
  </div>

  <div class="roadmap-section">
    <h3>计划中</h3>
    <ul class="roadmap-list planned">
      {roadmap.planned.map((item) => (
        <li>{item}</li>
      ))}
    </ul>
  </div>

  <div class="roadmap-section">
    <h3>已完成</h3>
    <ul class="roadmap-list done">
      {roadmap.done.map((item) => (
        <li>{item}</li>
      ))}
    </ul>
  </div>
</DocLayout>
